<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MediaPickerSheet" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MediaPickerSheet" parent="Base.Theme.MediaPickerSheet" />

    <style name="ExpandablePopupWindowAnimation" parent="Widget.AppCompat.PopupWindow">
        <item name="android:windowEnterAnimation">@anim/expand_in</item>
        <item name="android:windowExitAnimation">@anim/shrink_out</item>
    </style>

</resources>